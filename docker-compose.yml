# Generated by Athena v0.1.0 from MULTI_REACT_APPS deployment
# Developed by UNFAIR Team: https://github.com/Jeck0v/Athena
# Project Version: 1.0.0
# Generated: 2026-03-03 13:59:05 UTC
# Features: Intelligent defaults, optimized networking, enhanced health checks

# Services: 4 configured with intelligent defaults

services:
  front_screen:
    build:
      context: .
      dockerfile: apps/front-screen/Dockerfile
      args:
        NODE_ENV: dev
    container_name: multi-react-apps-front_screen
    ports:
    - 3002:80
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://localhost:80/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 128M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    networks:
    - react_apps_network
    pull_policy: missing
    labels:
      athena.project: MULTI_REACT_APPS
      athena.generated: 2026-03-03
      athena.service: front_screen
      athena.type: generic

  dmd_screen:
    build:
      context: .
      dockerfile: apps/dmd-screen/Dockerfile
      args:
        NODE_ENV: dev
    container_name: multi-react-apps-dmd_screen
    ports:
    - 3001:80
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://localhost:80/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.3'
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    networks:
    - react_apps_network
    pull_policy: missing
    labels:
      athena.service: dmd_screen
      athena.type: generic
      athena.generated: 2026-03-03
      athena.project: MULTI_REACT_APPS

  back_screen:
    build:
      context: .
      dockerfile: apps/back-screen/Dockerfile
      args:
        NODE_ENV: dev
    container_name: multi-react-apps-back_screen
    ports:
    - 3000:80
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://localhost:80/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.3'
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    networks:
    - react_apps_network
    pull_policy: missing
    labels:
      athena.type: generic
      athena.service: back_screen
      athena.project: MULTI_REACT_APPS
      athena.generated: 2026-03-03

networks:
  react_apps_network:
    driver: bridge

name: MULTI_REACT_APPS
