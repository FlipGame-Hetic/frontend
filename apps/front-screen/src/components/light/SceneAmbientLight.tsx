import { SCENE_AMBIENT_LIGHT_INTENSITY } from "./sceneAmbientLightConfig"

const SceneAmbientLight = () => {
  return <ambientLight intensity={SCENE_AMBIENT_LIGHT_INTENSITY} />
}

export default SceneAmbientLight
